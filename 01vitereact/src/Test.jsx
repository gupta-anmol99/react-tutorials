function TestIt(){
    return(
        <>
        <h2>This is a test script</h2>
        <h3>We cannot return more than one element from JSX so we use fragment that is, empty angular.</h3>
        <h4>This makes the return a single element.</h4>
        </>
    )
}

export default TestIt